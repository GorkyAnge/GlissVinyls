@model Resena

@{
    ViewData["Title"] = "Agregar Resena";
    Layout = "_LayoutUsuario";
}

<section id="general" class="contact">
    <div class="container">
        <div class="section-title">
            <h2>Publica Tus Reseñas</h2>
            <p>En esta página puedes agregar tu reseña sobre el álbum</p>

        </div>
        <form method="post">
            <div class="mb-3">
                <label class="form-label">Id Vinilo</label>
                <input asp-for="ViniloId" class="form-control" value="@ViewBag.IdProducto">
            </div>
            <div class="mb-3">
                <label class="form-label">Nombre Usuario</label>
                <input asp-for="Usuario" class="form-control" value="@ViewBag.Nombre" readonly>
            </div>
            <div class="mb-3">
                <label class="form-label">Escribe tu comentario:</label>
                <textarea asp-for="Texto" class="form-control" rows="3"></textarea>
            </div>
            <div class="col-6">
                <button asp-action="CreateResena" type="submit" asp-controller="Resena" class="btn btn-outline-success">Agregar Reseña</button>
                <a asp-action="Index" asp-controller="Resena" class="btn btn-outline-primary">Regresar</a>

            </div>
        </form>

    </div>
</section>


